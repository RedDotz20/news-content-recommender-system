'use client';

import { UserPreferenceBoundary } from './components/UserPreferenceBoundary';

export default function HomePageComponent() {
	return <UserPreferenceBoundary>PREFERENCES PRESENT</UserPreferenceBoundary>;
}
